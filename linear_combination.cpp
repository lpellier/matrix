#include "general.hpp"

// template <typename T>
// Vector<T>	linear_combination(std::vector<Vector<T>> vecs, std::vector<T> coefs) {
// 	if (vecs.size() == 0 || vecs.size() != coefs.size())
// 		return Vector<T>();
// 	Vector<T> ret(vecs.size());
// 	for (size_t i = 0; i < vecs.size(); i++) {
// 		ret.add(vecs[i].scl(coefs[i]));
// 	}
// 	return ret;
// }
